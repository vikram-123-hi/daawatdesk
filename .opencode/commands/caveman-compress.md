---
description: Compress a markdown memory file into caveman prose
---

Use the `caveman-compress` skill to compress this file: $ARGUMENTS

Only compress natural language files. Preserve code blocks, inline code, URLs, file paths, commands, identifiers, dates, version numbers, and markdown structure exactly.
